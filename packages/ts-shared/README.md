# ts-shared

该目录存放 TypeScript 共享契约，当前主要服务 `apps/web` 对 Python 结构化产物的只读解析。

当前覆盖类型：

- 候选人画像（`CandidateProfile`）
- 岗位画像（`JDProfile`）
- 简历版本（`ResumeVariant`）
- 评分卡（`ScoreCard`）
- 缺口映射（`GapMap`）
- 投递策略（`ApplicationStrategy`）
- 运行配置（`RunConfig`）
