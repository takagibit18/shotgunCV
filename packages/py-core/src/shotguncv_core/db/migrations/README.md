# ShotgunCV DB migrations

These migrations define the optional PostgreSQL projection used by `shotguncv index`, retrieval, and post-run review. The Python pipeline does not depend on these migrations for normal `run_dir` execution.
